<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_start_page"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="isleofdead.mohm.isleofdead.GameStartActivity"
    android:background="@drawable/background_game_start"
    >


    <Button
        android:text="New Game"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:id="@+id/newGame"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:id="@+id/loadGame"
        android:text="Load Game"
        android:visibility="invisible"
        android:layout_below="@+id/newGame"
        android:layout_alignStart="@+id/newGame"
        android:layout_alignEnd="@+id/newGame" />

    <Button
        android:text="Credits"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/loadGame"
        android:id="@+id/creditsButton"
        android:visibility="invisible"
        android:layout_alignStart="@+id/loadGame"
        android:layout_alignEnd="@+id/loadGame" />

    <Button
        android:text="Exit"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:id="@+id/exitGame"
        android:layout_below="@+id/creditsButton"
        android:layout_alignStart="@+id/creditsButton"
        android:layout_alignEnd="@+id/creditsButton" />

</RelativeLayout>
